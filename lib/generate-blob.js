/**
 * Generate a blob link.
 * @param {string} file - File name
 * @param {string} contents - Contents of the file
 * @param {string} sha - Commit where this todo was introduced
 */
module.exports = function generateBlob (file, contents, sha) {
  return file
}
